import discord
from discord.ext import commands

class GameMenu(commands.Cog):
    def __init__(self, bot):
        self.bot = bot

    @commands.command(name="게임", aliases=["게임목록", "games", "game"])
    async def show_game_list(self, ctx):
        """
        현재 봇에서 플레이 가능한 모든 게임 목록을 보여줍니다.
        사용법: #게임
        """
        embed = discord.Embed(
            title="🎮 멀티플레이 게임 라운지",
            description="친구와 함께 즐길 수 있는 게임 목록입니다.\n각 게임의 **명령어**를 입력하면 게임 링크가 생성됩니다.",
            color=0x00ff00  # 초록색 테두리
        )

        # ---------------------------------------------------------
        # 1. 커넥트 포 (이미 만드신 기능)
        # ---------------------------------------------------------
        embed.add_field(
            name="1️⃣ 커넥트 포 (Connect 4)", 
            value="> **명령어:** `#커넥트포`\n> **설명:** 칩을 4개 연속으로 놓으면 승리하는 두뇌 게임!", 
            inline=False
        )

        # ---------------------------------------------------------
        # 2. (추후 추가될 게임 예시 - 지금은 준비중으로 표시)
        # ---------------------------------------------------------
        embed.add_field(
            name="2️⃣ 오목 (Coming Soon)", 
            value="> **명령어:** `#오목` (준비중)\n> **설명:** 5개의 돌을 먼저 놓는 사람이 승리!", 
            inline=False
        )

        # ---------------------------------------------------------
        # 3. (추후 추가될 게임 예시)
        # ---------------------------------------------------------
        embed.add_field(
            name="3️⃣ 스피드 퀴즈 (Coming Soon)", 
            value="> **명령어:** `#퀴즈` (준비중)\n> **설명:** 가장 먼저 정답을 맞히는 사람이 점수 획득!", 
            inline=False
        )

        embed.set_footer(text="💡 원하는 게임의 명령어를 채팅창에 입력해보세요!")
        
        await ctx.send(embed=embed)

async def setup(bot):
    await bot.add_cog(GameMenu(bot))
